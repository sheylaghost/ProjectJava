/*
 Este programa em Java implementa o clássico jogo "Pedra, Papel e Tesoura".

 O jogador escolhe entre três opções:
    0 - Pedra
    1 - Papel
    2 - Tesoura

 O computador faz uma escolha aleatória e, em seguida, 
 o programa compara as jogadas para determinar o vencedor.

 Regras do jogo:
    - Pedra vence Tesoura
    - Tesoura vence Papel
    - Papel vence Pedra
    - Caso ambos escolham a mesma opção, o resultado é um empate.

 Este projeto treina conceitos básicos de programação em Java, 
 como entrada de dados com Scanner, geração de números aleatórios 
 com a classe Random, uso de arrays, estruturas condicionais 
 (if/else) e lógica de comparação.
*/

import java.util.Random;
import java.util.Scanner;

public class PedraPapelTesoura {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        Random random = new Random();

        // opções do jogo
        String[] opcoes = {"Pedra", "Papel", "Tesoura"};

        System.out.println("=== Jogo: Pedra, Papel e Tesoura ===");
        System.out.println("Digite sua escolha: 0 - Pedra | 1 - Papel | 2 - Tesoura");
        int escolhaJogador = scanner.nextInt();

        // jogada do computador
        int escolhaComputador = random.nextInt(3);

        System.out.println("Você escolheu: " + opcoes[escolhaJogador]);
        System.out.println("Computador escolheu: " + opcoes[escolhaComputador]);

        // regras
        if (escolhaJogador == escolhaComputador) {
            System.out.println("Empate!");
        } else if ((escolhaJogador == 0 && escolhaComputador == 2) ||
                (escolhaJogador == 1 && escolhaComputador == 0) ||
                (escolhaJogador == 2 && escolhaComputador == 1)) {
            System.out.println("Você venceu! 🎉");
        } else {
            System.out.println("Computador venceu! 😢");
        }

        scanner.close();
    }
}
